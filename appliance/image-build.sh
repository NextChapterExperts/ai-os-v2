#!/usr/bin/env bash
# Stub: später qcow2 / cloud-init Image-Build für Platform-VM (P19).
# Usage (Ziel): ./appliance/image-build.sh [--profile dev|prod]
set -euo pipefail

echo "TODO: image-build noch nicht implementiert."
echo "Phase 0: DEV-VM manuell (virt-manager) + Compose — siehe appliance/BOOTSTRAP-DEV-VM.md"
echo "cloud-init Vorlage: appliance/cloud-init/user-data"
exit 1
