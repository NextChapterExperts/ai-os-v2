# Copy to .env on first boot / appliance defaults (same keys as repo .env.example)
# Ollama: kein Secret — nur Host/Port/Modell
LITELLM_PORT=4000
OLLAMA_HOST=192.168.178.116
OLLAMA_PORT=11434
OLLAMA_DEFAULT_MODEL=qwen3.6-64k:latest
DEFAULT_COMPUTE_MODE=sovereign
# Nur für balanced/premium
OPENROUTER_API_KEY=
LANGFUSE_HOST=http://langfuse:3000
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_PG_PW=changeme-langfuse-pg
LANGFUSE_SECRET=changeme-langfuse-nextauth-secret-32chars
LANGFUSE_SALT=changeme-langfuse-salt
POSTGRES_HOST=postgres-platform
POSTGRES_DB=aios
POSTGRES_USER=aios
POSTGRES_PASSWORD=changeme-aios-pg
QDRANT_HOST=qdrant
QDRANT_PORT=6333
DEFAULT_TENANT=nextchapter
