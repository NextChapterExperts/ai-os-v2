# Minimal SearXNG settings for Phase-0 (LAN / DEV-VM)
use_default_settings: true
server:
  secret_key: "ai-os-dev-change-me"
  bind_address: "0.0.0.0"
  limiter: false
search:
  safe_search: 0
  formats:
    - html
    - json
